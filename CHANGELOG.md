# Changelog

本项目所有显著变更将记录在此文件中。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Security

**隧道协议升级到 v4：修复两个方向共用一把密钥导致的 nonce 重用**（⚠️ 两端必须成对升级）

v3 的 `DeriveSessionKey` 对收发两个方向派生**同一把**密钥，而两端的发送计数器都从 1
开始——于是客户端第 N 个包与服务端第 N 个包使用完全相同的 `(key, nonce)`。secretbox
底层是 XSalsa20 流密码，nonce 重用即密钥流重用（`C1⊕C2 = P1⊕P2`，而隧道里是 IP 包，
头部对能观察到 UDP 流的人高度可预测）；更严重的是 Poly1305 的一次性密钥取自密钥流
首块，已知明文即可恢复认证密钥，进而**伪造任一方向的数据包**。

- 改用 HKDF-SHA256 派生 c2s / s2c 两把方向密钥，info 绑定双方握手临时公钥
  （transcript binding：改了公钥的中间人即使伪造出合法 MAC 也派生不出同一把密钥）
- 加密算法换 ChaCha20-Poly1305（RFC 8439）：纯软件实现恒定快（客户端是用户的各种
  Windows 机器，不能假设有 AES-NI），nonce 24→12 字节，每包线上开销 41→25 字节
- 1 字节包类型进 AEAD 的附加数据：v3 的类型字节在加密盒之外、未受认证，一个合法
  Data 盒重标成 Ctrl 后仍能解开，类型混淆现已彻底关闭
- 服务端此前对 Pong **完全不解密**就刷新会话活跃时间——任何人伪造一个 `0x06` 字节
  就能给别人的会话续命（空闲回收永不触发）。现在 Ping/Pong 都走 AEAD 验证
- 握手 MAC 域标签升 v4；v3 客户端连 v4 服务端会得到明确的「请升级」日志而非
  查不出原因的认证失败，反向（v4 客户端连 v3 服务端）同样有明确文案

### Added

- **链路质量观测**（客户端面板新增一块）：丢包率、乱序率、抖动、往返延迟、纠错补回
  数、本地丢弃数。此前两端对丢包零观测——公网丢包、内核缓冲丢包、应用层丢弃在日志里
  混成一团，无法归因，后续所有丢包优化的参数都只能拍脑袋
- **前向纠错**（`tunnel.fec`，默认关闭）：每 8 个数据包附 1 个 XOR 校验包，组内丢 1 个
  可无损补回，省掉应用层重传的一个 RTT（跨网玩家 80~150ms）。XOR 的是**密文盒**而非
  明文，还原出的包必须走正常解密路径，认证与重放窗口一条不少；滑动组绝不主动等待，
  延迟代价为零。代价是下行冗余 12.5% 且隧道 MTU 让出 83 字节
- **小包冗余副本**（`tunnel.tail_dup`，默认关闭）：≤256 字节的包发两份（限频 20ms），
  接收端靠重放窗口免费去重。补的是纠错的盲区——组没满就没有校验包，而玩家操作指令与
  RakNet 探测恰好落在组尾
- **隧道 MTU 协商与路径探测**：服务端按「出口网卡 MTU − 封装开销」算出隧道 MTU 并在
  握手应答里下发（进 MAC），客户端同步设置虚拟网卡；另有被动探测（心跳载荷填满 +
  Pong 回显长度）在链路装不下时自动下调。固定 1400 在 PPPoE/4G/企业 VPN 上会贴上限，
  而 IP 分片丢一片等于整包全损
- **UDP 收发批量化**（`tunnel.io_mode`，默认 batch）：服务端两条数据泵改用
  recvmmsg/sendmmsg，把「每包 2 次系统调用」压到「每批 2 次」。批的边界就是一次系统
  调用返回多少包——不定时、不等待、不凑批，绝不为合并而引入延迟。非 Linux 自动降级为
  逐包并记一条日志；改成 `simple` 即可回退，不必换二进制
- **Linux UDP GRO/GSO**（`tunnel.udp_gro` / `tunnel.udp_gso`，默认关闭）：收益要等观测
  数据证明「批量化之后单核仍是瓶颈」才成立，GSO 还要求一条消息内各段等长而游戏流量
  包长参差不齐，所以实现进去但不默认启用
- **生成的配置文件自带每个配置项的中文注释**：文件本身就是速查文档，开一个开关不用
  再来回翻手册（用户实测开 `tunnel.fec` 时踩中这个往返成本）。升级合并写回时程序
  标准注释随用户值一起保留；注释文案与 README 字段讲解表同源，键序/缩进与旧版
  生成文件同构（升级 diff 只显示新增键）
- **配置文件带模式版本号（`version`），升级自动补全**：此前新增配置项只对
  「首次运行生成的新文件」可见——存量文件永远停在生成那天的样子，运维在文件里
  看不到有这些选项（缺失键在运行时按默认值生效，功能上没问题，只是不可见）。
  现在升级程序后首次启动会检测 `version`，低于当前版本时把「文件现有值 + 新默认值」
  合并写回：新增键补全、自定义值原样保留、原文件备份为 `config.yaml.v<N>.bak`、
  日志记一条升级说明。重写会把注释刷新为程序标准注释（自行追加的注释不保留，.bak
  可找回）。另外 `-config` 显式
  指向不存在的文件时改为在该路径生成默认文件（此前是启动报错）
- **面板新增「隧道链路质量」**（管理员）：「活跃会话」弹窗下半区展示每条在线隧道的
  丢包率（五档文案+配色：极好 0 / 高 <1% / 中 1~3% / 差 3~10% / 极差 ≥10%，两端与
  pf-client 口径一致）、乱序率、抖动、往返延迟、纠错补回、发送丢弃，以及服务端全局
  的内核收缓冲丢包、实际收发模式（io_mode）与隧道 MTU。端点
  `GET /api/tunnel/status` 仅管理员可见；隧道未开启时面板显示「隧道未开启」而非空表
- 服务端在线隧道视图（`PeerView`）新增协商 MTU 与该会话的链路质量数据

### Changed

- **热路径零分配**：封装/解密改为写入调用方缓冲，服务端入向把明文**直接解进 TUN 写
  批的槽位**。`SealData`/`OpenData` 基准从每包 2~3 次堆分配降到 0（1400 字节包实测
  0 B/op、0 allocs/op）
- **TUN 批量写**：`pkg/tunnet` 新增批量写接口。底层 `tun.Device.Write` 本来就是批量
  API（读侧早已吃到批量红利，写侧一直逐包），此前每包一次 make+copy+syscall
- **会话表改无锁快照**（copy-on-write）：三张索引合成不可变快照用原子指针发布，两条
  数据泵每包查表零锁零分配；写路径（握手/踢人/30 秒扫描）整体重建。来源地址索引的键
  从 `addr.String()` 换成 `netip.AddrPort`——字符串键要为**每个入向包**格式化一次 IP
  字符串，那是除加解密外最热的一处分配
- **客户端回程路由表 netip 化 + 热路径无锁**：条目键改 `netip.Addr`，删掉每包两次
  地址字符串分配；条目索引用原子快照发布，仅在增删条目（新玩家进服）时重建。快路径
  实测 28ns/包、0 allocs
- **客户端空闲重握手从 30 秒降到 10 秒**：NAT 换端口后服务端按设计静默丢弃，客户端
  只能靠读超时感知。服务端心跳 5 秒一个，10 秒 = 连续错过两个 + 余量；误判代价是多
  一次握手（微秒级），收益是最差恢复时间 30s→10s。同时不再每个入站包都重设读超时
- **回程路由安装缓冲 8 → 32 包**，并新增全局 8MB 字节闸门。8 个包在玩家进服瞬间
  （RakNet 打包密集 + route.exe 几十毫秒）很容易打穿，那就是「进服卡一下」；全局闸门
  挡的是伪造源地址洪泛——单 IP 上限拦不住 512 条目 × 32 包 × 1400 字节
- 内核 socket 缓冲生效值核查：程序请求 4MB 但受 `net.core.rmem_max` 钳制（多数发行版
  默认约 208KB ≈ 149 个满包），被钳制时给出具体的 sysctl 命令；另每 30 秒采样
  `/proc/net/udp` 的 drops，**仅在增量非零时**告警
- `pkg/tunnet` 读缓冲按设备规格分配并暴露 `ReadBufSize()`：调用方此前硬编码 1500，
  MTU 一提高就变成静默丢包
- 出向包超出隧道 MTU 时改为应用层显式判定 + 计数 + 限频告警。此前这类包在设备层被
  静默丢弃（计数器无人读取），是「后端 MTU 偏大时偶发大包全损」的黑盒来源

### Fixed

- **修复「升级重启后，上一次接入的玩家全部连不上、新玩家正常」**：客户端升级时进程
  被强制结束（任务管理器/脚本杀进程而非托盘退出），`cleanup` 没机会运行——上一轮为
  玩家装的 /32 回程路由留在 Windows 系统路由表里；新客户端的管理器不知道它们的存在，
  重连的旧玩家触发 `route add` 时被 Windows 以「对象已存在」拒绝，安装器从此无限退避，
  该 IP 的入站包缓冲满后全部丢弃。两道修复：① `route add` **幂等**——「对象已存在」
  按成功处理（路由已存在恰好就是想要的状态，直接收编）；② 客户端启动时**清扫**系统
  里仍以隧道网关为下一跳的残留 /32 路由（治本：强杀/断电/崩溃留下的路由此前永远没人
  清理，会让玩家之后不经代理直连源站也收不到回包）。清扫在管理器创建时同步执行
  （先于任何安装/回收，无并发窗口），按列解析 `route print -4`（不匹配文案，列序
  跨语言稳定），删除失败单条不阻断
- **协议版本不匹配不再显示「服务端无应答（请检查地址、端口与中转机防火墙）」**：
  此前版本不匹配在两端都是静默的，客户端只能靠超时，把版本问题误报成网络问题，
  运维会去白查防火墙。现在：客户端在 v4 握手全部无应答后用一条 v3 格式的探测包
  区分原因——「中转机服务端隧道协议版本过旧（v3）」（服务端未升级，升级后自动
  恢复）或「隧道协议版本不匹配」（两端版本不一致）；服务端则对版本过旧但 MAC
  验证通过的旧客户端回一个用旧版本域标签签名的拒绝应答（reason 0），旧客户端
  至少能看到「被服务端拒绝」并指向管理员。MAC 无效与未知访问码依旧静默——
  不引入访问码存在性探测口子，35 字节应答 < 122 字节请求，无反射放大；成本与既有
  认证失败路径同类（helloQ + 单 worker 天然限流）。新增
  `RejectVersionMismatch`（reason 6，终态）：未来版本的服务端用它告知 v4 客户端升级
- 写 TUN 失败与写 UDP 失败此前**共用同一个日志限频锚点且文案固定为「写入 TUN 失败」**：
  出向 socket 缓冲满时会打出一条指向错误方向的日志。现拆成两个锚点两条文案
- 客户端两处日志限频写成 `now-last < sec && !CAS(...)`：`now-last >= sec` 时短路跳过
  CAS，锚点永不更新，于是**每次调用都打印**——限频等于没做。现已修正并加测试锁住语义
- 客户端回程路由缓冲的入站包此前直接持有调用方缓冲的切片；改为零分配路径后该缓冲会
  被下一个包覆盖，现显式拷贝并加测试（不拷贝的话安装完成后代写出去的是「最后一个包
  重复 N 次」，这类 bug 在单包测试里完全看不出来）

## [v1.0.2] - 2026-09-03

### Changed

**数据面优化：消除「玩家加入 / 重试风暴 → 全服短暂卡顿」**（两端均需更新）

- 客户端：/32 回程路由安装移出隧道读循环——新玩家 IP 的首包改为缓冲，由后台
  安装器异步执行 route.exe 后按序代写；其他玩家的包全程不再被单个 IP 的安装
  阻塞（旧实现每个新来源 IP 让读循环停顿几十毫秒，即「有人进服全服卡一下」的
  客户端病根）；安装失败改指数退避重试，不再删除条目导致后续每包重演
- 服务端：隧道握手移出数据泵——Hello 由专用 worker 串行处理，存储 I/O 不再
  卡住全体玩家的入向包；设备指纹未变化的重握手短路 BindDevice，重试风暴不再
  每次握手写库（fsync）
- 两端：防重放滑动窗口改环形位图，判定 O(1)（旧实现顺序流量下每包做
  O(8192) 次 map 全表清理）；协议与线上报文格式零变化
- 服务端：活跃会话快照改读写锁，降低周期性快照与会话登记的锁争用
- 两端：隧道与转发 UDP socket 读写缓冲调至 4MB（默认 ~200KB，突发时在内核层
  丢包且应用层无感知）；Linux 实际生效值受 net.core.rmem_max/wmem_max 钳制，
  建议同时调大内核上限
- 两端：删除隧道热路径逐包冗余内存拷贝

> 修复记录：曾在 21dae91 中把透明模式回程的 relayBack（读透明 socket → 经转发口
> 发回玩家的最后一跳）依据过时注释误判为死代码删除，导致透明代理入向正常、
> 回程全灭（v1.0.1-1/v1.0.1-2 均受影响）；635bf3b 另修复了会话清理先删后关的
> EADDRINUSE 窗口。两者已随 635bf3b 之后的回退提交一并解决，UDP 转发器整体
> 回到 v1.0.1 行为（仅保留 socket 缓冲调优）。**受影响的部署请升级到
> v1.0.1-3 或更高。**

## [1.0.1] - 2026-09-02

**客户端无需更新**：本版本改动全部在服务端，pf-client 客户端代码零变化
（重新发布的客户端压缩包仅随版本号刷新，内容与 1.0.0 一致，现有安装可继续使用）。

### Fixed

- **修复全代理突然失联、重启服务器才恢复的根因**：ufw/宝塔等共存防火墙工具在
  reload 或增删规则时会整表 flush iptables（filter/nat/mangle 全清），把隧道回程
  只在启动时装配一次的内核规则（mangle 标记/策略路由/INPUT/FORWARD）连带清掉——
  症状为控制面照常、玩家入站照常、所有透明代理静默失联、日志零痕迹。现由守护
  协程每 30 秒幂等校验，缺失自动补装，并在日志留下「规则何时被清」的时间戳
- 隧道日志「丢弃源地址不匹配（疑似伪造）」不再误报：客户端 Windows 网卡的
  IPv6 等非 IPv4 后台流量降为 debug 记录；真实源地址伪造嫌疑仍保持 warn

## [1.0.0] - 2026-08-31

首个正式版本。基于上游 [shibingli/go-port-forward](https://github.com/shibingli/go-port-forward)
二次开发重构，自本版本起由 OWOSER 开发组维护。

### Added

**核心转发（上游基础 + 二开增强）**

- TCP/UDP 端口转发引擎：连接级与规则级双计数流量统计、IP 黑白名单、连接日志、系统诊断
- PROXY Protocol v2 真实 IP 透传（TCP + UDP）
- Web 管理界面（Alpine.js + Bootstrap 5，中英双语），前端资源嵌入二进制单文件分发

**透明模式隧道（二开新增）**

- 自研隧道组件：服务端集成进主程序（`tunnel.enabled`），另配 Windows 图形客户端 pf-client
- 规则级透明模式（Linux `IP_TRANSPARENT` 源绑定，仅 UDP），后端零插件即可看到玩家真实 IP
- 隧道协议 v3：按访问码独立密钥握手 + X25519 密钥交换 + NaCl 每包加密
- /32 回程路由完整生命周期：入站首包即时安装；服务端结束事件 20 秒宽限与本地空闲 90 秒兜底双驱动回收

**多用户体系（二开新增）**

- 全局设置 → 用户组 → 用户 → 访问码四级模型：配额以用户组为载体、全局设置为天花板
- 访问码实体化：独立隧道密钥、独立隧道地址、绑定一台设备
- 规则按用户归属，隧道数据面四条对称校验（入向/出向 × 源/目的）实现用户间硬隔离，网关零例外

**自助注册与邮件**

- 管理员可开关的自助注册；SMTP 邮箱验证码（用途绑定、限频、一次性）
- 账号页支持绑定/更换邮箱，未配置 SMTP 的存量账号补齐邮箱入口
- 找回密码流程防账号枚举

**Web 面板**

- 连接日志分页查看、删除/清空与每用户保留上限（管理员可配）
- 规则页配额速览条（当前分组/在线隧道/规则数量）、端口随机分配与占用检测
- 用户组在线编辑（组名/端口区间/三项配额）
- 中转机地址迁入全局设置，未配置时自动探测公网 IP

**Windows 客户端 pf-client**

- WebView2 原生窗口界面 + 系统托盘常驻，关窗仅隐藏
- 字节级流量统计（总量 / 每 IP / 实时速率）
- 单实例保护（命名互斥体）；启动失败原生 MessageBox 提示并写启动日志
- 自动放行虚拟网卡 Windows 防火墙入站规则

### Fixed

- 数据面 TUN offset 读写语义与透明回程路径两处硬伤（两次全局断流的根因）
- 回程路由残留导致开代理后无法直连源站、断开后概率性连不上
- 入站首包即时补齐回程路由，修复服务器列表探测等短交互超时
- Windows 已连接 UDP socket 统一用 `Write`（`WriteToUDP` 会静默丢包）
- Ctrl+C 后进程卡死不退出（阻塞读关停顺序 + 15 秒硬超时）
- 多实例互相抢占隧道会话与机器级全局资源（加单实例保护）
- 邮件正文改用标准库 quoted-printable 编码，修复中文乱码
- 通用模式域名目标拨号时复检解析结果，堵住 DNS 绕过黑名单的 SSRF
- 初始管理员密码不再写入滚动日志

### Security

- 通用模式与隧道彻底解耦：隧道网段地址一律拒绝并引导改用透明模式
- TUN 入站防火墙收紧为只放行回包，隧道用户不可经网关访问中转机服务
- 登录防爆破限频、验证码端点 per-IP 限频、500 错误脱敏
- 转发目标地址校验执行点下沉至数据面（API 预检之外增加拨号时复核）

### Documentation

- README 与客户端文档全面重写；配置文件讲解对齐生成器实际输出
